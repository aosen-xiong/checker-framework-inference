package checkers.inference.repair;

/** Edit proposal source used by an inference-repair experiment. */
public enum InferenceRepairEditProviderMode {
    DETERMINISTIC_ONLY,
    AI_ONLY,
    DETERMINISTIC_THEN_AI
}
